{
  "name": "server",
  "version": "1.0.0",
  "engines": {
    "node": "12.18.0"
  },
  "description": "",
  "main": "server.js",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.12.9"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd encryptNote && npm install --only=dev && npm install && npm run build",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC"
}
